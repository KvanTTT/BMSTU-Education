(defun model10 ()
  (setq Center(getpoint "\nEnter center "))
  (setq DBig(getdist "\nEnter D "))
  (setq dSmall(getdist "\Enter d "))
  (setq m(getdist "\Enter m "))
  (setq n(getdist "\Enter n "))

  (setq DMax(* m DBig))
  (setq DMin(* n DBig))
  (setq DMax2(/ DMax 2))
  (setq DMin2(/ DMin 2))
  (setq DBig2(/ DBig 2))
  (setq dSmall2(/ dSmall 2))
  (command "_circle" Center  DMax2)
  (cond ((/= DMin 0)
	 (command "_rectang" (list (- (car Center) DMin2) (- (cadr Center) DMin2)) (list (+ (car Center) DMin2) (+ (cadr Center) DMin2)))  
	 ))
  (setq c1(list (- (car Center) DBig2) (cadr Center)))
  (setq c2(list (+ (car Center) DBig2) (cadr Center)))
  (setq c3(list (car Center) (- (cadr Center) DBig2)))
  (setq c4(list (car Center) (+ (cadr Center) DBig2)))
  (command "_circle" c1  dSmall2)
  (command "_circle" c2  dSmall2)
  (command "_circle" c3  dSmall2)
  (command "_circle" c4  dSmall2)
  (setq c1(list (- (car c1) dSmall2)  (cadr c1)))
  (setq c2(list (+ (car c2) dSmall2)  (cadr c2)))
  (setq c3(list (car c3)  (- (cadr c3) dSmall2)))
  (setq c4(list (car c4)  (+ (cadr c4) dSmall2)))
  (setq c5(list (- (car Center) DMax2)  (cadr Center)))
  
  (command "_region" c1 c2 c3 c4 c5"")
  (command "_subtract" c5 ""c1 c2 c3 c4"")
  )